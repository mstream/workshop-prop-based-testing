import java.util.List;
import java.util.stream.Collectors;

class NastyIntegerSorter implements Sorter<Integer> {

    @Override
    public List<Integer> apply(List<Integer> listToSort) {
        return List.of();
    }
}
